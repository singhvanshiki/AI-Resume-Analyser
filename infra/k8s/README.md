# Kubernetes Manifests

This folder contains minimal deployment and service manifests for the platform. They are intended as a starting point and assume a single namespace with shared secrets and config maps.
